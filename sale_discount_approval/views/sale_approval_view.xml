<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_approval_log_list" model="ir.ui.view">
        <field name="name">sale.approval.log.list</field>
        <field name="model">sale.approval.log</field>
        <field name="arch" type="xml">
            <list string="Approval Logs">
                <field name="order_id"/>
                <field name="user_id"/>
                <field name="level"/>
                <field name="action"/>
                <field name="date"/>
            </list>
        </field>
    </record>
</odoo>